import { CONTACT_TITLE, LINKS_TITLE, TESTEMONIALS_TITLE } from "../../config/config";

const ContactSlide = () => {
    return (
        <div>
            <h1>{CONTACT_TITLE}</h1>
            <h3>{TESTEMONIALS_TITLE}</h3>
            <h3>{LINKS_TITLE}</h3>
        </div>
    );
};

export default ContactSlide;
