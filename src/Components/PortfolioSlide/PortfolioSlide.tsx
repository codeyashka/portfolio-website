import { PORTFOLIO_TITLE } from "../../config/config";

const PortfolioSlide = () => {
    return (
        <div>
            <h1>{PORTFOLIO_TITLE}</h1>
        </div>
    );
};

export default PortfolioSlide;
