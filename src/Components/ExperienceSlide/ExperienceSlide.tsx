import { EXP_TITLE } from "../../config/config";

const ExperienceSlide = () => {
    return (
        <div>
            <h1>{EXP_TITLE}</h1>
        </div>
    );
};

export default ExperienceSlide;
